export type RiskOwnerRow = {
  'Nature of Loss': string;
  'Primary (Operational functions that directly own & manage services and their associated risks)': string;
  'Secondary (Functions with oversight of management activity, separate from those responsible for delivery)': string;
};
